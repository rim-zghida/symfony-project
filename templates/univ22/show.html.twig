{% extends 'base.html.twig' %}

{% block title %}Univ22{% endblock %}

{% block body %}
    <h1>Univ22</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ univ22.id }}</td>
            </tr>
            <tr>
                <th>Teacher</th>
                <td>{{ univ22.teacher }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ univ22.email }}</td>
            </tr>
            <tr>
                <th>Adresses</th>
                <td>{{ univ22.adresses }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_univ22_index') }}">back to list</a>

    <a href="{{ path('app_univ22_edit', {'id': univ22.id}) }}">edit</a>

    {{ include('univ22/_delete_form.html.twig') }}
{% endblock %}
